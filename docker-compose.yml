version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: tokentrack-db
    environment:
      POSTGRES_DB: tokentrack
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: